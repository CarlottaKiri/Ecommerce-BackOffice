import { useEffect } from "react";
import styles from "./styles.module.scss";

const Home = () => {
  useEffect(() => {});
  return (
    <div className={styles.main}>
      <h1> Home</h1>
    </div>
  );
};

export default Home;
